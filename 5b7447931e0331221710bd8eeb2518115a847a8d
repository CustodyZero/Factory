# Phase 5.7 Code Review — Round 2

**Reviewer:** codex GPT-5.5 (read-only)
**Date:** 2026-05-03 (after one hang+retry on the codex side)
**Branch:** worktree-agent-a0b554550bc724180 (round-2 commits 41d2950, bd5763c)
**Verdict:** REQUEST-CHANGES

## Summary

Production fixes are structurally correct:
- Planner cost is recorded immediately after `invokeAgent` with `packet_id: null` and `spec_id` set.
- `readDayCost` now filters by record timestamp via `localDateFromTimestamp` after a bounded UTC-window candidate scan.

But one finding remains in the regression test fixtures.

## Issues

### 1. Round-2 regression tests still TZ-fragile in extreme east timezones

**File:** `tools/test/cost_io.test.ts:364` and similar fixtures.

The developer's `T00:00:00.000Z → T12:00:00.000Z` shift improved robustness for most TZs (UTC noon is unambiguous for UTC-12 through UTC+11). But at UTC+12 through UTC+14 (e.g., Pacific/Kiritimati), UTC noon already maps to the *next* local calendar day. So:

- `localDateFromTimestamp('2026-05-01T12:00:00.000Z')` returns `'2026-05-01'` in Phoenix/UTC/etc., but `'2026-05-02'` in Kiritimati.
- The regression test for the non-UTC day-cap bug therefore breaks in Kiritimati — exactly the kind of environment it claims to protect against.

Per CLAUDE.md §4 ("Behavior must not depend on locale, timezone..." — applies equally to tests since CI is just another environment, §4.5), the regression test must be TZ-invariant.

**Fix:** Build fixture timestamps from local calendar-date constructors so they anchor to local noon on the target date, regardless of TZ:

```ts
// Before (TZ-fragile in UTC+12..+14):
const ts = '2026-05-01T12:00:00.000Z';

// After (TZ-invariant):
const ts = new Date(2026, 4, 1, 12).toISOString();  // local noon on 2026-05-01

// And for the queried day:
const localDay = localDateFromTimestamp(ts);  // derive, don't hardcode
```

For the bug-specific test (record's local date = today, run-id filename = different UTC date), pick the run-id prefix from `utcDateWindow(localDay)` and explicitly assert the prefix is NOT equal to `localDay`. That makes the test prove the actual property without depending on a specific TZ.

## What this round did well

- Planner-cost fix exercises a real orchestrator planning path, not a direct `recordCost` call — the test pins the wire-up, not just the helper.
- Production `readDayCost` now uses the same local-date model as the orchestrator's day-cap gate, so the gate and the aggregator agree about which records belong to "today."

## Round budget remaining

1 round. Fix is narrow (test fixtures only); APPROVE expected after round 3.

# Phase 5.7 Code Review — Round 3

**Reviewer:** codex GPT-5.5 (read-only)
**Date:** 2026-05-03 (after one hang+retry on the codex side, fourth in this session's review chain)
**Branch:** worktree-agent-a0b554550bc724180 (round-3 commit 25ed976)
**Verdict:** APPROVE

## Summary

Round 3 is clean. Fixup touches only `tools/test/cost_io.test.ts` and `tools/test/cost_caps.test.ts`; production code is unchanged.

- Grep for UTC-anchored fixture literals is clean (zero remain).
- `new Date(y, m, d, h).toISOString()` constructs instants anchored to local wall-clock time, so `localDateFromTimestamp()` round-trips to the intended local day across TZs.
- Bug-specific tests now derive `localDay` from the fixture timestamp and explicitly assert the chosen UTC filename prefix differs from that local date — pinning the property, not a specific UTC date.
- The mixed run-file test's `windowA ∩ windowB \ {dayA}` selection is correct: run-file B is visited for both day queries while still exercising the non-matching prefix case.

## Strongest parts

- Tests now pin the intended property instead of a specific UTC date.
- The intersection fix closes the extreme-east timezone hole without touching production code.

## Round budget

3 of 3 rounds used. Bounded-iteration model honored.

# Phase 5.7 QA Verification

**QA identity:** separate Opus agent (FI-7 distinct from developer and orchestrator)
**Date:** 2026-05-03
**Verdict:** APPROVE

## Acceptance criteria results

All 14 acceptance criteria PASS, all 16 specific checks PASS.

| # | Criterion | Result |
|---|-----------|--------|
| 1 | Per-invocation cost capture (planner + dev + reviewer + QA) | PASS — recordCost at plan_phase.ts:155, develop_phase.ts:270, verify_phase.ts:134; null tokens/dollars where provider doesn't report |
| 2 | 3 cap scopes optional in factory.config | PASS — schema has per_run/per_packet/per_day, all optional |
| 3 | cost.cap_crossed emitted before abort | PASS — develop_phase.ts:282-294 and orchestrator.ts:772-781 both emit BEFORE break/return |
| 4 | Per-run / per-packet / per-day escalation correct | PASS — per-packet sets currentPoint=null & continues; per-run breaks spec loop & emits pipeline.failed; per-day records block file |
| 5 | Per-day uses LOCAL date | PASS — localDateString uses getFullYear/getMonth/getDate; orchestrator pre-flight at :553 |
| 6 | Pure/I/O boundary | PASS — zero fs imports in pipeline/cost.ts |
| 7 | Provider-agnostic | PASS — extractTokens covers codex/claude/copilot/default with null fallback |
| 8 | Test coverage | PASS — cost.test.ts + cost_caps.test.ts + cost_io.test.ts |
| 9 | Schema doc-only | PASS — cost_record.schema.json not wired into validate.ts |
| 10 | InvokeResult shape preserved | PASS — 485 tests still pass |
| 11 | All tests pass | PASS — 485 tests, 34 files |
| 12 | validate.ts PASS | PASS |
| 13 | No new deps | PASS — package.json identical |
| 14 | Forward-compat for Phase 6 | PASS — CostRecord carries run_id, packet_id, spec_id at every call site |

## Facade-risk check (CLAUDE.md §3.1, §3.5)

**No silent zeros.** Null-dollar records sum into `unknown_count` (not 0) at `aggregateDollars` (cost.ts:313-318), surface in `run.ts:120/124` ("N unknown-cost invocations"), and are excluded from cap accounting at `develop_phase.ts:275-277` via explicit `if (cost.dollars !== null)` guard. Honest unknowns throughout the chain.

## TZ-invariance independent verification

`TZ=Pacific/Kiritimati npx vitest run` → 485/485 pass. The bug-protection contract holds in extreme east TZs.

## Strongest parts (independent assessment)

- Pure/I/O boundary is genuinely clean: `pipeline/cost.ts` zero fs imports, with `localDateFromTimestamp` and `utcDateWindow` in the pure layer composed by I/O-side `readDayCost`.
- TZ-invariance hardened in two layers: round 2 fixed the silent under-reporting bug; round 3 made the regression tests immune to system TZ. `TZ=Pacific/Kiritimati` clean.

# Phase 5.7 Orchestrator Approval

**Orchestrator identity:** Opus session (separate from developer and QA per FI-7)
**Date:** 2026-05-03
**Verdict:** APPROVE — ready for merge

## Summary

Phase 5.7 ships per-invocation cost tracking + 3-scope caps. Phase 6 recovery now has its retry budget substrate.

- 7 commits: 09f1998, 8ce487f, 1fa21fc, c3f5c3f, 41d2950, bd5763c, 25ed976
- New files: tools/pipeline/cost.ts (340 lines pure + 101 added in round 2), tools/cost.ts (270 + 52 added round 2 = 322 I/O), schemas/cost_record.schema.json (doc-only)
- Modified: events.ts (+98), agent_invoke.ts (+93), develop_phase.ts (+128), verify_phase.ts (+85), orchestrator.ts (+147), run.ts (+27), config.ts (+54), plan_phase.ts (+54 round 2), factory-config.schema.json (+28), event.schema.json (+5)
- tests: 410 → 485 (+75 across three rounds)
- validate.ts: PASS, package.json unchanged, schemas additive (not wired)
- 2 rounds of REQUEST-CHANGES (round 1: planner cost gap + day-cap UTC bug; round 2: TZ-fragile fixtures), 1 round of APPROVE
- Independent QA (separate Opus, FI-7) APPROVE on all 14 criteria with TZ-invariance independently verified under UTC+14

## Round budget

3 of 3 rounds used. Bounded-iteration model honored. Ship.

## Operational note

Codex CLI hung 4 of 6 times during the review chain (process startup network stall — 0% CPU, 0 bytes output, never times out client-side). Kill+retry succeeded every time. This is external (codex CLI/network reliability), not factory. The post-5.7 orchestrator review checkpoint is the right place to consider whether the review workflow should add a wrapper/timeout discipline.
