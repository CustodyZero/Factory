## Codex GPT-5.5 Review — Phase 4.5

**Reviewer:** codex CLI v0.125.0 with --model gpt-5.5
**Reviewed branch:** worktree-agent-a967d4c8c0425299f vs main
**Date:** 2026-05-01

### Verdict
APPROVED-WITH-NITS

### "No I/O in pipeline/" invariant evaluation — ENDORSED
Phase 1's "no I/O in pipeline/" invariant was over-stated for the pre-Phase-4.5 reality. The single_entry_pipeline.md decision places phase modules under tools/pipeline/, and phases legitimately do I/O. The relaxation is correct architectural correction, not erosion. No need to split into tools/phases/ or pipeline/pure/ — that would add ceremony without solving a current problem.

### No-behavior-change verification — PASSES
Compared the deleted planPhase, devPhase, qaPhase, invokeAgent, safeCall, refreshCompletionId, and resolveRunArg bodies in the diff against the new modules.

- runPlanPhase preserves existing-feature short-circuit, prompt construction, dry-run null result, provider/tier selection, planner invocation, stderr truncation, feature re-read, intent patch, and log messages.
- runDevelopPhase preserves packet scan, completion refresh, topo sort, dependency failure behavior, dry-run continue behavior, developer/reviewer/rework invocations, lifecycle calls, force-approve fallback, completion identity, and result arrays.
- runVerifyPhase preserves QA filtering, completion short-circuit, dependencies + verifies blocking, dry-run continue behavior, QA invocation, startPacket, completePacket, and skipped/failed/completed semantics.
- run.ts still calls plan, develop, verify in the same order and preserves feature status/summary behavior.

### runVerifyPhase rename — FINE
No runtime caller still references qaPhase. rg only finds qaPhase in spec/docs/comments explaining the rename.

### Helper placement — REASONABLE (one nit)
refreshCompletionId and safeCall belong in lifecycle_helpers.ts. patchJson is borderline: run.ts has one copy AND plan_phase.ts has a private duplicate. Not blocking but a future cleanup should put it in a tiny JSON helper if touched again.

### Test quality — GOOD
Spot-checked develop_phase, verify_phase, plan_phase, phases_integration, run, agent_invoke, lifecycle_helpers tests. They cover deterministic imperative branches: existing-completion, blocked dependency, dry-run, empty feature, cross-phase callability. Some "exports callable" tests are low-value but a small part of overall coverage.

Targeted Phase 4.5 tests in codex sandbox: 72 passed.
Full npm test in sandbox: 259 passed, 23 failed — all from spawned npx tsx CLI tests returning status: null / SecItemCopyMatching failed -50. Sandbox env issue, not a Phase 4.5 regression. Local invocation in worktree confirms 282/282 passing.

### resolveRunArg extraction — ACCEPTABLE
Practically necessary for the under-350-line target (run.ts now 315; resolve_arg.ts 96). Slightly entry-ish rather than phase-ish, but Phase 5's driver/orchestrator will likely absorb this routing logic anyway.

### New issues introduced
None found.

### Required changes
None.

### Suggested improvements (nits, optional)
1. Adjust comments claiming the spawn path is covered by integration tests, or add a fake-provider test later; current tests avoid real spawn paths.
2. Consider extracting patchJson to a small shared helper only if another patching use appears.
3. Update old comments saying "devPhase / qaPhase" to "develop/verify phase" where convenient.

---

## QA Verification — Phase 4.5

**QA agent:** separate Opus invocation (FI-7 separate identity)
**Date:** 2026-05-01

### Verification verdict
ALL-PASS — every required criterion (1-11) and every spot-check (12-14) verified independently.

### Highlights
- npm test: 282 passing across 25 test files (Phase 4 baseline 253 + 29 new tests)
- tools/run.ts: 315 lines (well under 350 target; down from 773)
- Three new phase modules exist with documented exports:
  - tools/pipeline/plan_phase.ts (runPlanPhase)
  - tools/pipeline/develop_phase.ts (runDevelopPhase + Phase 1's pure functions retained)
  - tools/pipeline/verify_phase.ts (runVerifyPhase, renamed from qaPhase)
- No-behavior-change verified by reading runPlanPhase / runDevelopPhase / runVerifyPhase against the original planPhase/devPhase/qaPhase line-for-line
- Phase 1's pure functions retained (deriveDevResumePoint, nextPointAfter*, DevResumePoint type at lines 51-142 of develop_phase.ts)
- Phase 1's pure helpers retained in agent_invoke.ts (resolveModelId, buildProviderArgs); imperative invokeAgent added alongside
- runVerifyPhase rename clean: no caller references qaPhase as a function (only doc-string mentions explaining the rename)
- patchJson stayed in run.ts (single use site for the coordinator)
- refreshCompletionId and safeCall moved to tools/pipeline/lifecycle_helpers.ts
- No new external dependencies; validate.ts PASS

### Behavioral spot-check
- run() in run.ts: parses args, calls resolveRunArg, then runPlanPhase, runDevelopPhase, runVerifyPhase in order, then renders summary (verified at run.ts:125-268)
- lifecycle_helpers.ts exists with both helpers (refreshCompletionId at L57, safeCall at L88)
- agent_invoke.ts docblock at L7-32 explicitly acknowledges the I/O-invariant relaxation with rationale; invokeAgent at L164 uses spawnSync as expected

### Independent observations
- The four commits each map cleanly to one extraction step — reviewable in sequence
- runPlanPhase returns { feature_id: string | null } instead of returning null directly on plan failure. Equivalent control flow, slightly cleaner contract; caller updated at run.ts:181
- main (the previous merge commit) has 271 tests + 1 failing in QA's environment; this branch has 282 + 0 failing. Either an incidentally-fixed flaky test or a sandbox-specific failure on main. Either way, the branch is unambiguously green.

---

## Orchestrator Approval — Phase 4.5

**Orchestrator:** this session
**Date:** 2026-05-01

### Decision
APPROVED for merge to main, pending human go-ahead.

### Rationale
- Codex GPT-5.5 round 1: APPROVED-WITH-NITS — verified no behavior change by reading each phase function's diff carefully; endorsed the "no I/O in pipeline/" invariant relaxation as architectural correction
- Independent Opus QA (FI-7 separate identity): ALL-PASS on 11 acceptance criteria + 3 behavioral spot-checks; verified Phase 1's pure functions retained, Phase 4.5's imperative additions placed correctly
- Single-round phase: no required changes from codex. The developer's first pass was clean — substantial refactor (458-line reduction in run.ts) with no behavior change

### What lands
- 4 new modules under tools/pipeline/: plan_phase, verify_phase, lifecycle_helpers, resolve_arg
- Existing tools/pipeline/develop_phase.ts extended with imperative runDevelopPhase alongside Phase 1's pure functions
- tools/pipeline/agent_invoke.ts gains the imperative invokeAgent (was in run.ts); pure helpers retained
- tools/run.ts shrunk from 773 to 315 lines (-458). The architectural "entry layer" is now thin
- +29 new tests across phase modules, integration tests, and helper tests

### Architectural call
The developer relaxed Phase 1's "no I/O in tools/pipeline/" invariant to accommodate the imperative phase modules per single_entry_pipeline.md's 4-layer architecture. Codex endorsed this as architectural correction. The new effective invariant: tools/pipeline/ contains both pure helpers AND imperative phase modules; pure helpers are the simpler ones (topo, prompts, agent_invoke pure helpers, develop_phase's decision functions); imperative phase modules do I/O for their own scope. No need to split into tools/phases/ or pipeline/pure/ — that would add ceremony without solving a current problem.

This is recorded here in the git notes so future contributors understand the invariant evolved deliberately.

### Iteration record
- Round 1 (4 commits, +29 tests): APPROVED-WITH-NITS, no required changes
- Total: 4 commits, +29 tests (253 → 282), run.ts 773 → 315 (-458)

### What's deliberately deferred
- Nits from codex (3 cosmetic items: spawn-path test coverage, patchJson consolidation, old-naming comments) — small enough to defer; would not affect correctness if left

### Practical implication for the architecture
The 4-layer architecture from single_entry_pipeline.md is now realized:
- Entry: tools/run.ts (315 lines — thin coordinator)
- Driver: deferred to Phase 5 (multi-spec orchestrator)
- Phases: tools/pipeline/{plan_phase, develop_phase, verify_phase}.ts
- Lifecycle: tools/lifecycle/* (Phase 3)

Phase 5+ work lands on a clean architectural foundation rather than piling onto a god script.
