# Phase 6 (revised) Code Review — Round 2

**Reviewer:** codex GPT-5.5 (read-only)
**Date:** 2026-05-05
**Branch:** worktree-agent-ae1b27e7f8caf42df (round-2 commits 691683e, c4b0c89, d769e91)
**Verdict:** REQUEST-CHANGES (codex explicitly: "I do not recommend revert")

## What's confirmed correct

- `RecoveryResult<T>` discriminator architecture preserved.
- `FailureScenario` closed enum stayed at the original 8 variants. `CascadedFromDependency` is a label, NOT an enum addition.
- Atomic completePacket contract: no record / no status mutation when ci_pass=false.
- Develop finalize BuildFailed retry now invokes the dev agent with `attempt.guardrailPrompt` BEFORE re-running completePacket.
- Mock fixtures reflect "no record on failed CI."
- LintFailed/TestFailed/CompletionGateBlocked/ProviderUnavailable still escalate immediately.

## Issues remaining (schema-validator drift only)

### 1. `validate.ts` `validStatuses` doesn't include `"failed"`

**File:** `tools/validate.ts:127`

`schemas/packet.schema.json` now allows `"failed"`, and recovery writes it. But `validate.ts` (the authoritative validator that `npx tsx tools/validate.ts` runs) still has the old `validStatuses` array without it. A real post-escalation artifact would fail validation even though it's a normal terminal state.

**Fix:** Add `"failed"` to `validStatuses`. Add a `validate.test.ts` fixture proving a realistic failed packet validates.

### 2. Recovery writes a `failure` object that the schema rejects

**Files:** `schemas/packet.schema.json:103`, `tools/pipeline/develop_phase.ts:362`, `tools/pipeline/verify_phase.ts:340`

`packet.schema.json` has `additionalProperties: false`, but both escalation and QA cascade paths stamp a `failure` object onto packet artifacts. Real recovery output is therefore outside the declared data model.

**Fix:** Add an optional `failure` object to `packet.schema.json`. Document its shape (scenario, reason, escalation_path or similar). Document `CascadedFromDependency` as a packet-failure label string (NOT a `FailureScenario` enum member).

## What this round did well

- The atomic completePacket contract is the right architectural call — completion records now mean "this packet is verified done," which simplifies the whole reasoning model.
- The finalize BuildFailed retry now genuinely invokes remediation before re-attempting verification (regression-fixed from the previous attempt).

## Round budget remaining

1 round. The two findings are narrow schema/validator drift fixes. APPROVE expected after round 3.

# Phase 6 (revised) Code Review — Round 3 (FINAL)

**Reviewer:** codex GPT-5.5 (read-only)
**Date:** 2026-05-05
**Branch:** worktree-agent-ae1b27e7f8caf42df (round-3 commits b0b034f, 8484dc6)
**Verdict:** APPROVE

## Summary

No blocking findings. Both round-2 issues fixed cleanly:

- `tools/validate.ts:132` accepts `status: "failed"` and validates the optional `failure` object shape when present.
- `schemas/packet.schema.json:151` declares `failure` under the top-level `additionalProperties: false` schema, with required `scenario` + `reason`, optional `attempts` + `escalation_path`, and nested `additionalProperties: false`.

## Cross-checks performed

- Write sites match the schema: develop escalation (`develop_phase.ts:362`), verify escalation (`verify_phase.ts:197`), QA cascade (`verify_phase.ts:340` — writes `CascadedFromDependency`, `attempts: 0`, `escalation_path: null`).
- `FailureScenario` remains the 8-member closed type at `recovery.ts:66`. `CascadedFromDependency` is only a packet failure label, not a recipe key.
- `'Unclassified'` is legitimate: schema names it; recovery loop escalates unknown classifications immediately at `recovery_loop.ts:315`.

## Non-blocking follow-up observations

- `failure` is optional, so `status: "failed"` without `failure` isn't currently rejected by the schema/validator despite the description implying they're paired. Could be tightened in a future schema update.
- No direct `validate.test.ts` fixture for `failure.scenario: "Unclassified"`; it validates by design because `scenario` is intentionally an open non-empty string.

## Strongest improvements over the previous (reverted) attempt

- Escalation is now **controlling**, not just observable, through the `RecoveryResult<T>` discriminator (compile-time enforcement of state-machine dispatch).
- Schema, validator, and recovery write sites now share a concrete failure artifact shape instead of drifting silently.

## Round budget

3 of 3 rounds used. Bounded-iteration model honored.

# Phase 6 (revised) QA Verification

**QA identity:** separate Opus agent (FI-7 distinct from developer and orchestrator)
**Date:** 2026-05-05
**Verdict:** APPROVE

All 17 acceptance criteria PASS, all 17 specific checks PASS.

## Critical verifications

- **8 `runWithRecovery` wrap sites verified** to have `kind === 'escalated'` dispatch + `markPacketFailed` + `break`/`continue` pattern. No log-then-advance bug class anywhere.
- **Atomic completePacket verified at source** (`complete.ts:233-261`): on !ci_pass, early-return with `already_complete: false`, no record write, no status mutation. Eliminates the false-success short-circuit at the data layer.
- **Round-3 reverted bug class provably gone** (develop_phase.ts:611-622 vs prior log-then-advance): every escalated branch calls markPacketFailed AND breaks/continues.
- **TZ-invariance preserved**: `TZ=Pacific/Kiritimati` 630/630 pass.
- **Closed enum stayed at 8 variants**. `CascadedFromDependency` and `Unclassified` are documented schema-level label strings, NOT enum members.

## Strongest improvements over the reverted attempt

1. **Discriminator is *controlling*, not *cosmetic*.** Every wrap site forces compile-time dispatch on escalation; the type system makes "logged-then-advances" structurally impossible.
2. **Atomic completePacket removes the false-success short-circuit at the source.** A failed verification can no longer be retried-into-success because no completion record exists for the retry to short-circuit on.

# Phase 6 (revised) Orchestrator Approval

**Orchestrator identity:** Opus session (separate from developer and QA per FI-7)
**Date:** 2026-05-05
**Verdict:** APPROVE — ready for merge

## Summary

Phase 6 (revised) ships the recovery layer with state-machine integration as a first-class architectural concern. After 1 reverted attempt + 3 review rounds on the revised attempt, the architecture is clean and the contract holds end-to-end.

- 12 commits: foundation/wiring (7) + round 2 (3) + round 3 (2)
- New files: `tools/pipeline/recovery.ts` (630 pure), `tools/pipeline/recovery_loop.ts` (690 orchestration), `tools/recovery.ts` (118 I/O), `tools/lifecycle/git_check.ts` (211)
- Modified: develop_phase.ts (+402/-82), verify_phase.ts (+253/-31), plan_phase.ts (+71/-15), events.ts (+157/-2), lifecycle/{request_review,complete}.ts (+91 total), complete.ts CLI (+17), execute.ts (+65), status.ts (+42), validate.ts (+33), packet.schema.json (+27)
- tests: 485 → **630** (+145 across 12 commits)
- validate.ts: PASS, package.json unchanged
- 3 review rounds (REQUEST-CHANGES, REQUEST-CHANGES, APPROVE)
- Independent QA APPROVE on all 17 acceptance criteria + 17 specific checks

## Architectural breakthrough vs prior attempt

The reverted attempt's defect: recovery escalation was *observable* (events fired) but *not controlling* (the per-packet state machine ignored escalation and advanced anyway). The revised attempt's typed `RecoveryResult<T>` discriminator forces compile-time dispatch — every wrap site MUST handle `kind === 'escalated'` to typecheck, and the standard pattern (`markPacketFailed` + `break`/`continue`) is consistently applied across 8 wrap sites.

Plus the atomic `completePacket` contract: completion records exist only when the packet truly verified successfully. No more false-success short-circuit.

## Round budget

3 of 3 rounds used. Bounded-iteration model honored.

## Operational note

This is the second consecutive Phase 6 review chain. The first attempt was reverted after round 3 RECOMMEND-REVERT (logged-then-advances bug class). The revised attempt's success comes from naming state-machine integration as a first-class architectural concern instead of treating it as wiring — exactly the lesson recorded in `docs/research/phase_6_recovery_attempt.md`.
