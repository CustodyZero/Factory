# DEP0190 Mini-Spec Code Review — Round 2

**Reviewer:** codex GPT-5.5 (read-only)
**Date:** 2026-05-11
**Branch:** worktree-agent-ad052c3e79cbdd690 (round-2 commit 89e0bad)
**Verdict:** APPROVE

## Both round-1 issues fixed

- Command contract now explicitly supports bare executables AND absolute paths with spaces, with macOS/Windows examples. `"gh copilot --"` called out as the unsupported legacy multi-arg shape with the rewrite target.
- Risk table now names the concrete Windows `.cmd`/`.bat` spawn failure and gives two viable mitigation paths (POSIX-only support boundary OR cross-platform spawn-boundary test exercising wrapper shims).

## Confirmed intact

- 3-phase plan unchanged.
- Dual-acceptance migration story preserved.
- References still resolve.

## Non-blocking observation

If implementation chooses POSIX-only support boundary, the completion evidence should state that native Windows support is intentionally out of scope so the "cross-platform" test language is not overread.

## Strongest parts

- Clarifies the core argv contract.
- Turns the Windows concern into a concrete implementation decision instead of a vague platform risk.

## Round budget

2 of 3 rounds used. Bounded-iteration model honored.

# DEP0190 Mini-Spec QA Verification

**QA identity:** separate Opus agent (FI-7 distinct from developer and orchestrator)
**Date:** 2026-05-11
**Verdict:** APPROVE

All 11 acceptance criteria PASS, all 12 sub-checks PASS.

## Critical verifications

- Frontmatter matches `single-entry-pipeline.md` shape (id, title, depends_on: [])
- All 8 sections present and structured
- 3 phases each have explicit Deliverables + Acceptance
- Risk table covers all 5 risk categories including Windows `.cmd`/`.bat` shim and operator migration friction
- Command contract section explicitly supports bare-name AND absolute-path-with-spaces (macOS + Windows examples)
- Unsupported legacy shape (`"gh copilot --"`) explicitly called out as the migration boundary
- Acceptance criteria measurable
- Tests + validate unaffected (713 pass, validate reports 2 specs)
- No code changes; only `specs/dep0190-shell-removal.md`
- No emojis

## Facade audit

- Dual-acceptance migration story is load-bearing AND required (not just mentioned): L74 "accepts BOTH shapes", L129-141 Phase 1 deliverables, L192 risk row marks it load-bearing.
- Windows shim decision explicitly deferred to the implementing developer with an explicit binary choice (POSIX-only support boundary OR cross-platform shim test). Honest deferral, not hidden punt.
- No partial-success patterns.

## Strongest aspects (independent assessment)

1. **Windows `.cmd`/`.bat` risk row is exceptionally well-scoped**: names the concrete failure mode (`spawn` on Windows resolves only `.exe`-style natively; npm/Scoop/Chocolatey ship `.cmd` shims), then forces an explicit, documented binary choice on the implementing developer. No false neutrality.
2. **The legacy-shape migration boundary is unambiguous**: states exactly which previously-working string breaks under `shell: false`, exactly why, and exactly what the operator must do. The contract between "supported" and "migration boundary" is the kind of explicit, reviewable seam §3.2 asks for.

# DEP0190 Mini-Spec Orchestrator Approval

**Orchestrator identity:** Opus session (separate from developer and QA per FI-7)
**Date:** 2026-05-11
**Verdict:** APPROVE — ready for merge

## Summary

Mini-spec scoping the DEP0190 `shell:true` deprecation fix. 2 commits. 250 lines. Spec-only.

- 2 review rounds (codex REQUEST-CHANGES with Windows shim + absolute-paths-clarity → codex APPROVE)
- Independent QA APPROVE on all 11 criteria + 12 sub-checks
- This is a PLANNING artifact; implementation lands in a future packet/spec invocation

## Round budget

2 of 3 rounds used. Bounded-iteration model honored.
